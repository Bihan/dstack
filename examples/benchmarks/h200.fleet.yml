type: fleet
# The name is optional, if not specified, generated randomly
name: h200-fleet

# Uncomment if instances are interconnected
#placement: cluster

# SSH credentials for the on-prem servers
ssh_config:
  user: dstack
  identity_file: ~/.ssh/id_rsa
  hosts:
      hostname: 74.80.190.167
